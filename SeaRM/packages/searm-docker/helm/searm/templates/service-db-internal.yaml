{{- if .Values.db.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "searm.fullname" . }}-db
  namespace: {{ include "searm.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "searm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "searm.name" . }}
    app.kubernetes.io/component: db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
{{- end }}
