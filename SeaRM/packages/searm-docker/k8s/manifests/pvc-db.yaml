apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: searmcrm-db-pvc
  namespace: searmcrm
spec:
  storageClassName: default
  volumeName: searmcrm-db-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
