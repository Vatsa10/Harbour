apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: searmcrm-server
  name: searmcrm-server
  namespace: searmcrm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: searmcrm-server
  template:
    metadata:
      labels:
        app: searmcrm-server
    spec:
      volumes:
        - name: searmcrm-server-data
          persistentVolumeClaim:
            claimName: searmcrm-server-pvc
        - name: searmcrm-docker-data
          persistentVolumeClaim:
            claimName: searmcrm-docker-data-pvc
      containers:
        - name: searmcrm
          image: searmcrm/searm:latest
          imagePullPolicy: Always
          env:
            - name: NODE_PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@searmcrm-db.searmcrm.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://searmcrm-redis.searmcrm.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: searmcrm-docker-data
            - mountPath: /app/packages/searm-server/.local-storage
              name: searmcrm-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
