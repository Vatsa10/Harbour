---
title: CLI
description: أوامر yarn searm لتنفيذ الدوال، وبثّ السجلات، وإدارة تثبيتات التطبيقات، والتبديل بين الريموتات.
icon: terminal
---

تُعد واجهة سطر الأوامر `yarn searm` وسيلتك للتعامل مع كل ما يتعلق بالتطبيق. القائمة الكاملة للأوامر:

| أمر                                             | ماذا يفعل                                                                            | موثَّق في                                                                                           |
| ----------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| `dev`                                           | يراقب ملفات المصدر ويزامن التغييرات مباشرة                                           | [البدء السريع](/l/ar/developers/extend/apps/getting-started/quick-start)                                 |
| `الخطة`                                         | معاينة تغييرات البيانات الوصفية بدون تطبيقها                                         | [المزامنة والاستعادة](/l/ar/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `تطبيق`                                         | تطبيق تغييرات البيانات الوصفية بعد عرض الخطة                                         | [المزامنة والاستعادة](/l/ar/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | ترجمة التطبيق وإنشاء عميل واجهة برمجة التطبيقات (استخدم `--tarball` لحزم ملف `.tgz`) | [النشر](/l/ar/developers/extend/apps/operations/publishing)                                              |
| `dev:typecheck`                                 | تشغيل فحص الأنواع في TypeScript                                                      | [الاختبار](/l/ar/developers/extend/apps/operations/testing)                                              |
| `dev:add`                                       | إنشاء هيكل كيان جديد                                                                 | [إنشاء الهياكل](/l/ar/developers/extend/apps/getting-started/scaffolding)                                |
| `dev:generate-client`                           | إعادة إنشاء عميل واجهة برمجة التطبيقات مضبوط الأنواع                                 | هذه الصفحة                                                                                          |
| `dev:function:exec` / `dev:function:logs`       | تنفيذ الدوال وبث سجلاتها                                                             | هذه الصفحة                                                                                          |
| `dev:translations-extract`                      | استخراج السلاسل القابلة للترجمة إلى كتالوجات `locales/`                              | [الترجمات](/l/ar/developers/extend/apps/translations/overview)                                           |
| `dev:catalog-sync`                              | تشغيل مزامنة كتالوج السوق                                                            | [النشر](/l/ar/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)              |
| `app:publish` / `app:install` / `app:uninstall` | دورة حياة الإصدارات                                                                  | [النشر](/l/ar/developers/extend/apps/operations/publishing) وهذه الصفحة                                  |
| `docker:*`                                      | إدارة حاوية خادم SeaRM المحلي                                                       | [الخادم المحلي](/l/ar/developers/extend/apps/getting-started/local-server)                               |
| `remote:*`                                      | إدارة اتصالات الخادم                                                                 | هذه الصفحة                                                                                          |

كل أمر يقبل الوسيط `-r, --remote \<name>` لاستهداف خادم بعيد معيَّن بدلاً من الخادم الافتراضي.

## تنفيذ الدوال (`yarn searm dev:function:exec`)

تشغيل دالة منطقية يدويًا دون تشغيلها عبر HTTP أو cron أو حدث قاعدة بيانات:

```bash filename="Terminal"
# Execute by function name
yarn searm dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn searm dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn searm dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn searm dev:function:exec --postInstall
yarn searm dev:function:exec --preInstall
yarn searm dev:function:exec --uninstall
```

## عرض سجلات الدوال (`yarn searm dev:function:logs`)

بثّ سجلات التنفيذ لدوال تطبيقك المنطقية:

```bash filename="Terminal"
# Stream all function logs
yarn searm dev:function:logs

# Filter by function name
yarn searm dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn searm dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
يختلف هذا عن `yarn searm docker:logs`، الذي يعرض سجلات حاوية Docker. يعرض `yarn searm dev:function:logs` سجلات تنفيذ دوال تطبيقك من خادم SeaRM.
</Note>

## توليد العميل محدد الأنواع (`yarn searm dev:generate-client`)

أعد توليد عميل واجهة برمجة التطبيقات محدد الأنواع (`searm-client-sdk`) من مخطط الجهة البعيدة النشطة، دون بناء تطبيق أو مزامنته. استخدمه للحصول على عميل محدد الأنواع في أي مشروع — مثل خدمة خلفية موجودة في مستودع منفصل — يتواصل مع مثيل SeaRM الخاص بك:

```bash filename="Terminal"
# In your project (no SeaRM app definition required)
yarn add searm-sdk searm-client-sdk

# Connect to the SeaRM instance to generate the client from
yarn searm remote:add

# Generate the typed client into node_modules/searm-client-sdk
yarn searm dev:generate-client
```

ثم استورد العميل في شيفرتك:

```typescript
import { CoreApiClient } from 'searm-client-sdk/core';
```

أعد تشغيل الأمر كلما تغيّر نموذج البيانات لديك لتحديث الأنواع المُولَّدة.

<Note>
يتم إنشاء العميل البرمجي داخل `node_modules`، لذا لا يُدرج مع شيفرتك في الالتزامات (commits). شغّل `yarn searm dev:generate-client` بعد كل عملية تثبيت (على سبيل المثال في سكربت `postinstall` أو في CI).
</Note>

## إلغاء تثبيت تطبيق (`yarn searm app:uninstall`)

أزل تطبيقك من مساحة العمل النشطة:

```bash filename="Terminal"
yarn searm app:uninstall

# Skip the confirmation prompt
yarn searm app:uninstall --yes
```

## إدارة الريموتات

**الريموت** هو خادم SeaRM يتصل به تطبيقك. أثناء الإعداد، تُنشئ أداة إنشاء الهيكل واحدًا لك تلقائيًا. يمكنك إضافة ريموتات أخرى أو التبديل بينها في أي وقت.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn searm remote:add

# Connect to a local SeaRM server (auto-detects port 2020 or 3000)
yarn searm remote:add --local

# Add a remote non-interactively (useful for CI)
yarn searm remote:add --url https://your-searm-server.com --api-key $SEARM_API_KEY --as my-remote

# List all configured remotes
yarn searm remote:list

# Set the active remote
yarn searm remote:use <name>

# Check that the active remote's authentication is still valid
yarn searm remote:status

# Remove a remote
yarn searm remote:remove <name>
```

تُخزَّن بيانات اعتمادك في `~/.searm/config.json`.
