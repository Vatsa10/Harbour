---
title: الخادم المحلي
description: إدارة خادم SeaRM المحلي المستند إلى Docker — بدء التشغيل، الإيقاف، الترقية، مثيل اختبار متوازٍ، وإعداد SDK اليدوي.
icon: server
---

## إدارة الخادم المحلي

استخدم `yarn searm docker:*` للتحكّم في حاوية SeaRM المحلية:

| أمر                                    | ماذا يفعل                                          |
| -------------------------------------- | -------------------------------------------------- |
| `yarn searm docker:start`             | بدء تشغيل الخادم (يسحب الصورة إذا لزم الأمر)       |
| `yarn searm docker:start 2.2.0`       | بدء إصدار محدد من الخادم                           |
| `yarn searm docker:start --port 3030` | بدء التشغيل على منفذ مخصّص                         |
| `yarn searm docker:stop`              | إيقاف الخادم (مع الحفاظ على البيانات)              |
| `yarn searm docker:status`            | عرض عنوان URL والإصدار وبيانات اعتماد تسجيل الدخول |
| `yarn searm docker:logs`              | بث سجلات الخادم                                    |
| `yarn searm docker:reset`             | مسح البيانات والبدء من جديد                        |
| `yarn searm docker:upgrade`           | سحب أحدث صورة `searm-app-dev`                     |
| `yarn searm docker:upgrade 2.2.0`     | الترقية إلى إصدار محدد                             |

تظل البيانات محفوظة عبر عمليات إعادة التشغيل في وحدتي تخزين Docker (`searm-app-dev-data` لـ PostgreSQL، و`searm-app-dev-storage` للملفات). استخدم `reset` لمسح كل شيء.

## تثبيت إصدار الخادم

عند عدم تمرير أي إصدار، يقوم `docker:start` باشتقاق الإصدار من النطاق `engines.searm` في ملف `package.json` لتطبيقك — وهو نفس النطاق الذي يتحقق منه الخادم عند تثبيت تطبيقك. يشغّل أحدث صورة منشورة لـ `searm-app-dev` التي تُلبي هذا النطاق، مع الرجوع إلى `latest` عندما يكون الحقل غير موجودًا أو لا يتطابق أي إصدار منشور:

```json filename="package.json"
{
  "engines": {
    "searm": ">=2.2.0"
  }
}
```

مرِّر إصدارًا بشكلٍ صريح لتجاوز النطاق لتشغيلٍ واحد فقط: `yarn searm docker:start 2.3.0`. إذا كانت هناك حاوية موجودة بالفعل على إصدار مختلف، يقوم `docker:start` بترقيتها في مكانها (مع إعادة إنشاء الحاوية مع الحفاظ على وحدات تخزين بياناتك).

## ترقية صورة الخادم

يقوم `yarn searm docker:upgrade` بسحب أحدث صورة، ومقارنة التجزئات، ولا يعيد إنشاء الحاوية إلا إذا حدث تغيير فعلي. تظل وحدات التخزين محفوظة — ويتم استبدال الحاوية فقط. إذا تم سحب صورة جديدة وكانت الحاوية تعمل، فستبدأ عملية الترقية تلقائيًا حاوية جديدة؛ شغّل بعد ذلك `yarn searm docker:start` للانتظار حتى تصبح سليمة.

```bash filename="Terminal"
yarn searm docker:upgrade            # Latest
yarn searm docker:upgrade 2.2.0      # Specific version
```

تحقّق من الإصدار الجاري باستخدام `yarn searm docker:status` (يعرض قيمة `APP_VERSION` المضمنة في الحاوية).

## تشغيل مثيل اختبار متوازٍ

مرّر `--test` إلى أي أمر `docker:*` لإدارة مثيل ثانٍ معزول تمامًا — مفيد لاختبارات التكامل أو للتجربة من دون لمس بيانات التطوير الرئيسية لديك:

| أمر                                 | ماذا يفعل                                 |
| ----------------------------------- | ----------------------------------------- |
| `yarn searm docker:start --test`   | بدء مثيل الاختبار (المنفذ الافتراضي 2021) |
| `yarn searm docker:stop --test`    | إيقافه                                    |
| `yarn searm docker:status --test`  | عرض حالته                                 |
| `yarn searm docker:logs --test`    | بث سجلاته                                 |
| `yarn searm docker:reset --test`   | مسح بياناته                               |
| `yarn searm docker:upgrade --test` | ترقية صورته                               |

يملك مثيل الاختبار حاويته الخاصة (`searm-app-dev-test`) ووحدات التخزين الخاصة به (`searm-app-dev-test-data`، `searm-app-dev-test-storage`) وتهيئته — ويعمل جنبًا إلى جنب مع مثيلك الرئيسي بدون تعارضات. اجمع `--test` مع `--port` لتجاوز المنفذ 2021.

## إعداد يدوي (بدون أداة توليد الهيكل)

تجاوز أداة توليد الهيكل إذا كنت تضيف SDK إلى مشروع قائم:

```bash filename="Terminal"
yarn add searm-sdk searm-client-sdk
```

أضِف النص البرمجي إلى `package.json`:

```json filename="package.json"
{
  "scripts": {
    "searm": "searm"
  }
}
```

يمكنك الآن تشغيل `yarn searm dev`، و`yarn searm docker:start`، والبقية.

<Note>
لا تثبّت `searm-sdk` عالميًا — ثبِّته لكل مشروع بحيث يستخدم كل تطبيق إصداره الخاص.
</Note>
