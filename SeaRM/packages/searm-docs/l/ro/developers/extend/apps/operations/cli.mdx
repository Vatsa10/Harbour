---
title: CLI
description: comenzi `yarn searm` pentru executarea funcțiilor, transmiterea în flux a jurnalelor, gestionarea instalărilor de aplicații și comutarea între remote-uri.
icon: terminal
---

Interfața CLI `yarn searm` este punctul tău de acces pentru tot ce ține de aplicație. Lista completă de comenzi:

| Comandă                                         | Ce face                                                                                   | Documentat în                                                                                              |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `dev`                                           | Monitorizează fișierele sursă și sincronizează în timp real modificările                  | [Ghid de pornire rapidă](/l/ro/developers/extend/apps/getting-started/quick-start)                              |
| `plan`                                          | Previzualizează modificările metadatelor fără a le aplica                                 | [Sincronizare și recuperare](/l/ro/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `aplică`                                        | Aplică modificările metadatelor după afișarea planului                                    | [Sincronizare și recuperare](/l/ro/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | Compilează aplicația și generează clientul API (`--tarball` pentru a împacheta un `.tgz`) | [Publicare](/l/ro/developers/extend/apps/operations/publishing)                                                 |
| `dev:typecheck`                                 | Rulează verificarea tipurilor TypeScript                                                  | [Testare](/l/ro/developers/extend/apps/operations/testing)                                                      |
| `dev:add`                                       | Creează scheletul unei entități noi                                                       | [Generarea scheletului](/l/ro/developers/extend/apps/getting-started/scaffolding)                               |
| `dev:generate-client`                           | Regenerează clientul API tipizat                                                          | această pagină                                                                                             |
| `dev:function:exec` / `dev:function:logs`       | Execută funcții și transmite în flux jurnalele acestora                                   | această pagină                                                                                             |
| `dev:translations-extract`                      | Extrage șirurile traducibile în cataloagele din `locales/`                                | [Traduceri](/l/ro/developers/extend/apps/translations/overview)                                                 |
| `dev:catalog-sync`                              | Declanșează o sincronizare a catalogului marketplace-ului                                 | [Publicare](/l/ro/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)                 |
| `app:publish` / `app:install` / `app:uninstall` | Ciclul de viață al lansărilor                                                             | [Publicare](/l/ro/developers/extend/apps/operations/publishing) și această pagină                               |
| `docker:*`                                      | Administrează containerul serverului SeaRM local                                         | [Server local](/l/ro/developers/extend/apps/getting-started/local-server)                                       |
| `remote:*`                                      | Administrează conexiunile la server                                                       | această pagină                                                                                             |

Fiecare comandă acceptă `-r, --remote \<name>` pentru a viza un anumit server la distanță în locul celui implicit.

## Executarea funcțiilor (`yarn searm dev:function:exec`)

Rulați manual o funcție logică fără a o declanșa prin HTTP, cron sau eveniment de bază de date:

```bash filename="Terminal"
# Execute by function name
yarn searm dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn searm dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn searm dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn searm dev:function:exec --postInstall
yarn searm dev:function:exec --preInstall
yarn searm dev:function:exec --uninstall
```

## Vizualizarea jurnalelor funcțiilor (`yarn searm dev:function:logs`)

Transmiteți în flux jurnalele de execuție pentru funcțiile logice ale aplicației:

```bash filename="Terminal"
# Stream all function logs
yarn searm dev:function:logs

# Filter by function name
yarn searm dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn searm dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
Acest lucru este diferit de `yarn searm docker:logs`, care afișează jurnalele containerului Docker. `yarn searm dev:function:logs` afișează jurnalele de execuție ale funcțiilor aplicației de pe serverul SeaRM.
</Note>

## Generarea clientului tipizat (`yarn searm dev:generate-client`)

Regenerați clientul tipizat al API-ului (`searm-client-sdk`) din schema remote-ului activ, fără a construi sau sincroniza o aplicație. Folosiți-l pentru a obține un client tipizat în orice proiect — cum ar fi un serviciu backend aflat într-un repository separat — care comunică cu instanța dvs. SeaRM:

```bash filename="Terminal"
# In your project (no SeaRM app definition required)
yarn add searm-sdk searm-client-sdk

# Connect to the SeaRM instance to generate the client from
yarn searm remote:add

# Generate the typed client into node_modules/searm-client-sdk
yarn searm dev:generate-client
```

Apoi importați clientul în codul dvs.:

```typescript
import { CoreApiClient } from 'searm-client-sdk/core';
```

Rulați din nou comanda ori de câte ori se schimbă modelul de date pentru a reîmprospăta tipurile generate.

<Note>
Clientul este generat în interiorul `node_modules`, astfel încât nu este inclus în commit împreună cu codul dvs. Rulați `yarn searm dev:generate-client` după fiecare instalare (de exemplu, într-un script `postinstall` sau în CI).
</Note>

## Dezinstalarea unei aplicații (`yarn searm app:uninstall`)

Eliminați aplicația din spațiul de lucru activ:

```bash filename="Terminal"
yarn searm app:uninstall

# Skip the confirmation prompt
yarn searm app:uninstall --yes
```

## Gestionarea remote-urilor

Un „remote” este un server SeaRM la care se conectează aplicația. În timpul configurării, Scaffolderul creează automat unul pentru dvs. Puteți adăuga mai multe remote-uri sau comuta între ele oricând.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn searm remote:add

# Connect to a local SeaRM server (auto-detects port 2020 or 3000)
yarn searm remote:add --local

# Add a remote non-interactively (useful for CI)
yarn searm remote:add --url https://your-searm-server.com --api-key $SEARM_API_KEY --as my-remote

# List all configured remotes
yarn searm remote:list

# Set the active remote
yarn searm remote:use <name>

# Check that the active remote's authentication is still valid
yarn searm remote:status

# Remove a remote
yarn searm remote:remove <name>
```

Acreditările dvs. sunt stocate în `~/.searm/config.json`.
