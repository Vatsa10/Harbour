---
title: Yerel Sunucu
description: Yerel SeaRM Docker sunucusunu yönetin — başlatın, durdurun, yükseltin, paralel test örneği çalıştırın ve manuel SDK kurulumu yapın.
icon: server
---

## Yerel sunucuyu yönetme

Yerel SeaRM konteynerini kontrol etmek için `yarn searm docker:*` kullanın:

| Komut                                  | Ne yapar                                                  |
| -------------------------------------- | --------------------------------------------------------- |
| `yarn searm docker:start`             | Sunucuyu başlatır (gerekirse imajı çeker)                 |
| `yarn searm docker:start 2.2.0`       | Belirli bir sunucu sürümünü başlatın                      |
| `yarn searm docker:start --port 3030` | Özel bir portta başlatır                                  |
| `yarn searm docker:stop`              | Sunucuyu durdurur (verileri korur)                        |
| `yarn searm docker:status`            | URL'yi, sürümü ve oturum açma kimlik bilgilerini gösterir |
| `yarn searm docker:logs`              | Sunucu günlüklerini akış olarak gösterir                  |
| `yarn searm docker:reset`             | Verileri siler ve sıfırdan başlatır                       |
| `yarn searm docker:upgrade`           | En güncel `searm-app-dev` imajını çeker                  |
| `yarn searm docker:upgrade 2.2.0`     | Belirli bir sürüme yükseltir                              |

Veriler, yeniden başlatmalar arasında iki Docker biriminde kalıcıdır (PostgreSQL için `searm-app-dev-data`, dosyalar için `searm-app-dev-storage`). Her şeyi silmek için `reset` kullanın.

## Sunucu sürümünü sabitleme

Herhangi bir sürüm iletilmediğinde, `docker:start` sürümü uygulamanızın `package.json` içindeki `engines.searm` aralığından çözümler — bu, uygulamanız yüklendiğinde sunucunun doğruladığı aralıkla aynıdır. Bu komut, aralığı karşılayan en yeni yayımlanmış `searm-app-dev` imajını başlatır; alan yoksa veya hiçbir yayımlanmış sürüm eşleşmiyorsa `latest` değerine geri döner:

```json filename="package.json"
{
  "engines": {
    "searm": ">=2.2.0"
  }
}
```

Aralığı tek bir çalıştırma için geçersiz kılmak amacıyla sürümü açıkça iletin: `yarn searm docker:start 2.3.0`. Bir kapsayıcı zaten farklı bir sürümde mevcutsa, `docker:start` onu yerinde yükseltir (veri birimlerinizi korurken kapsayıcıyı yeniden oluşturarak).

## Sunucu imajını yükseltme

`yarn searm docker:upgrade`, en güncel imajı çeker, özetleri karşılaştırır ve yalnızca gerçekten bir şey değiştiyse konteyneri yeniden oluşturur. Birimler korunur — yalnızca konteyner değiştirilir. Yeni bir imaj çekildiyse ve konteyner çalışıyorsa, yükseltme otomatik olarak yeni bir konteyner başlatır; sağlıklı hale gelmesini beklemek için ardından `yarn searm docker:start` çalıştırın.

```bash filename="Terminal"
yarn searm docker:upgrade            # Latest
yarn searm docker:upgrade 2.2.0      # Specific version
```

Çalışan sürümü `yarn searm docker:status` ile doğrulayabilirsiniz (konteynere gömülü `APP_VERSION` değerini gösterir).

## Paralel bir test örneği çalıştırma

Ana geliştirme verilerinize dokunmadan entegrasyon testlerini veya denemeleri yürütmek için, herhangi bir `docker:*` komutuna `--test` parametresini vererek ikinci, tamamen yalıtılmış bir örneği yönetebilirsiniz:

| Komut                               | Ne yapar                                                      |
| ----------------------------------- | ------------------------------------------------------------- |
| `yarn searm docker:start --test`   | Test örneğini başlatır (varsayılan bağlantı noktası 2021'dir) |
| `yarn searm docker:stop --test`    | Durdurur                                                      |
| `yarn searm docker:status --test`  | Durumunu gösterir                                             |
| `yarn searm docker:logs --test`    | Günlüklerini akış olarak gösterir                             |
| `yarn searm docker:reset --test`   | Verilerini siler                                              |
| `yarn searm docker:upgrade --test` | İmajını yükseltir                                             |

Test örneği, kendine ait bir Docker konteynerinde (`searm-app-dev-test`), ayrılmış birimlerle (`searm-app-dev-test-data`, `searm-app-dev-test-storage`) ve yapılandırmayla çalışır; böylece ana örneğinizle çakışma olmadan paralel olarak çalışabilir. Varsayılan 2021'i geçersiz kılmak için `--test` ile `--port`'u birlikte kullanın.

## Manuel kurulum (iskelet oluşturucu olmadan)

SDK'yı mevcut bir projeye ekliyorsanız iskelet oluşturma adımını atlayın:

```bash filename="Terminal"
yarn add searm-sdk searm-client-sdk
```

`package.json` dosyasına betiği ekleyin:

```json filename="package.json"
{
  "scripts": {
    "searm": "searm"
  }
}
```

Artık `yarn searm dev`, `yarn searm docker:start` ve diğerlerini çalıştırabilirsiniz.

<Note>
`searm-sdk`'yi global olarak kurmayın — her uygulama kendi sürümünü kullansın diye proje bazında sabitleyin.
</Note>
