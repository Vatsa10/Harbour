import { type InputJsonSchema } from 'searm-sdk/logic-function';

export const discordListGuildsInputSchema: InputJsonSchema = {
  type: 'object',
  properties: {},
  required: [],
  additionalProperties: false,
};
