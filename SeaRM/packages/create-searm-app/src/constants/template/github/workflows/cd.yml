name: CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [labeled]

permissions:
  contents: read

env:
  SEARM_DEPLOY_URL: http://localhost:2020

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-and-install:
    if: >-
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.label.name == 'deploy')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Deploy
        uses: Vatsa10/Harbour/.github/actions/deploy-searm-app@main
        with:
          api-url: ${{ env.SEARM_DEPLOY_URL }}
          api-key: ${{ secrets.SEARM_DEPLOY_API_KEY }}

      - name: Install
        uses: Vatsa10/Harbour/.github/actions/install-searm-app@main
        with:
          api-url: ${{ env.SEARM_DEPLOY_URL }}
          api-key: ${{ secrets.SEARM_DEPLOY_API_KEY }}
