# Repair report — Phase 5 plan + Phases 2–5 program document

**Verified against:** `d:/Files/Vatsa/Projects/AI-CRM/twenty` at commit `dba03d0907` (`git log --oneline -1` → `dba03d0907 style(ai-write-approval): apply oxfmt to the fix-wave changes`). This is at or after the review's stated `c6e057906b` baseline, and the `ai-write-approval` directory noted as "in flux" was re-read fresh at this commit (`proposal.resolver.ts`, `metadata-resolver.decorator.ts`).

Files edited: `2026-08-05-phase-5-vertical-apps.md`, `2026-08-05-phases-2-5-program.md`. No file under `packages/` was written, only read.

## Phase 5 findings

**C11 — `installWorkflowDefinition` is metadata-schema-only. FIXED.**
Verified `packages/twenty-server/src/engine/api/graphql/graphql-config/decorators/metadata-resolver.decorator.ts` — `@MetadataResolver()` tags a resolver class with `RESOLVER_SCHEMA_SCOPE_KEY: 'metadata'`, routing it onto the metadata GraphQL schema. Verified `twenty-client-sdk` ships two distinct generated clients at `packages/twenty-client-sdk/src/core/generated/` and `.../metadata/generated/` (`CoreApiClient` vs `MetadataApiClient`). Changed `seedWorkflow(client: CoreApiClient, ...)` → `seedWorkflow(client: MetadataApiClient, ...)` in Task 9 Step 1; both workflow-template files now take `MetadataApiClient`; `post-install.ts` (now written in Task 9 Step 4, see I23) constructs both a `CoreApiClient` (for `createSupportQueue`) and a `MetadataApiClient` (for the two `seed*Workflow` calls). Confirmed Phase 4's plan file (`2026-08-05-phase-4-agent-api-semantics.md:2600`) already puts `WorkflowTemplateResolver` under `@MetadataResolver()` — no mismatch to flag there, Phase 5 was the only side wrong. `installWorkflowDefinition` itself does not exist in the generated clients yet (confirmed by grep — zero hits), consistent with Phase 4 being unimplemented; this is expected and unrelated to the fix.

**C12 — app's service role has no `WORKFLOWS` permission flag. FIXED.**
Verified `packages/twenty-shared/src/constants/PermissionFlagType.ts:9` — `WORKFLOWS` is a real enum member. Verified `packages/twenty-shared/src/constants/SystemPermissionFlag.ts:10` — its universal identifier is `'6189e7bd-4051-5752-b6b1-5f31358fbaf1'`. Verified `SettingsPermissionGuard(PermissionFlagType.X)` real usage pattern across 10+ resolvers, including `ProposalResolver` at `.../ai-write-approval/resolvers/proposal.resolver.ts:26`. Verified the real `RoleConfig`/`RoleManifest` property name is `permissionFlagUniversalIdentifiers` (`packages/twenty-sdk/src/sdk/define/roles/role-config.ts:12`), and found a **live production precedent doing exactly this fix**: `packages/twenty-apps/public/people-data-labs/src/roles/default-function.role.ts:40` sets `permissionFlagUniversalIdentifiers: [SystemPermissionFlag.WORKFLOWS]` for an app whose post-install hook installs a workflow. Applied the identical pattern to Task 1 Step 3's `app-default.role.ts`, importing `SystemPermissionFlag` from `twenty-sdk/define` (verified real export at `packages/twenty-sdk/src/sdk/define/index.ts:162`). Added the review's recommended assertion to Task 11: a new test queries `workflows { name statuses }` filtered to the two template names and asserts both exist with `'ACTIVE'` in `statuses`.

**C13 — workflow steps carry no `id`/`valid`. FIXED, via the review's named alternative (add the fields to `WorkflowStepTemplate` and set them in both Phase 5 templates, since `installDefinition`'s normalization is Phase 4 scope, out of reach here).**
Verified the real `WorkflowAction` base schema at `packages/twenty-shared/src/workflow/schemas/base-workflow-action-schema.ts` — required fields are `id` (UUID), `name`, `valid` (boolean), optional `nextStepIds` (UUID array or null), optional `position`. Added `id: string`, `valid: true`, `nextStepIds: string[] | null` to `WorkflowStepTemplate` in Task 9 Step 1 with an inline comment citing the source file and explaining why `InstallWorkflowDefinitionInput.steps` being `GraphQLJSON` means nothing catches a missing `id` at compile time. Added two new fixed UUID constants to Task 1 Step 2 (`NEW_TICKET_TRIAGE_STEP_UNIVERSAL_IDENTIFIER`, `SLA_RISK_SWEEP_STEP_UNIVERSAL_IDENTIFIER`) and used them as each single-step template's `id`, with `valid: true` and `nextStepIds: null` (both templates are single-step, so there is nothing to chain — documented inline).

**I21 — agent id hedge in the workflow step. FIXED.**
Both workflow-template files now do an unconditional `client.query({ agents: { __args: { filter: { universalIdentifier: { eq: SUPPORT_TRIAGE_AGENT_UNIVERSAL_IDENTIFIER } } }, id: true } })` before building the template, throw if no agent is found, and use the resolved `agentId`. Removed the coin-flip language entirely. Could not verify the exact `agents` query field name/shape against a live schema (`AgentEntity`'s GraphQL DTO does not exist on disk yet — see below), so a caveat was added alongside the existing "confirm field names against a running instance" note already present in Task 11 Step 1, extended to cover the new `workflows`/`agents` field names too.

**I22 — Task 8 numbering starts at "Step 2". FIXED.**
Task 8 renumbered to Step 1 (uninstall.ts) → Step 2 (validate/commit). This was made easier by I23's fix, which also removed the old Steps 3–4 from Task 8 entirely (moved to Task 9).

**I23 — Task 8 commits a file that can't typecheck, against Global Constraints. FIXED.**
Moved the entire `post-install.ts` write (including the `seedNewTicketTriageWorkflow`/`seedSlaRiskSweepWorkflow` calls) out of Task 8 into Task 9 as a new Step 4, positioned after both workflow-template files exist (so their imports resolve). Task 9's old Steps 4–5 (typecheck, commit) renumbered to Steps 5–6. Task 8 now only writes and commits `uninstall.ts`, which has zero forward dependency and typechecks standalone — its Step 2 runs `yarn typecheck` for real instead of skipping it.

**I24 — role-to-agent binding sufficiency is inferred, not verified. PARTIALLY RESOLVED — code search performed, question left open, fallback applied per the review's own instructions.**
Read `packages/twenty-server/src/engine/core-modules/application/application-manifest/services/compute-application-manifest-all-universal-flat-entity-maps.service.ts` and `.../converters/from-agent-manifest-to-universal-flat-role-target.util.ts` (the actual converter that turns a `defineAgent`'s `roleUniversalIdentifier` into an installed role-target row). Neither reads or checks `canBeAssignedToAgents` — the converter builds the row unconditionally from whatever `roleUniversalIdentifier` is supplied. This is consistent with "the flag doesn't gate anything at this stage" but does not prove it, since a manifest-sync validation pass could still enforce it elsewhere, and Phase 4 (which owns agent-manifest install code) has not shipped — `AgentEntity` does not exist anywhere under `packages/twenty-server/src/engine/metadata-modules/` yet (confirmed: directory search returned nothing). Applied the review's fallback: added a new Task 11 test asserting the installed agent has a truthy `roleId` after install, with an inline note that the exact field name is unverified pending Phase 4's `AgentDTO`.

**N7 — `STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS.workspaceMember` is answerable. FIXED.**
Verified `packages/twenty-shared/src/metadata/constants/standard-object-universal-identifiers.constant.ts:26` → `workspaceMember: '20202020-3319-4234-a34c-82d5c0e881a6'`, re-exported unchanged through `packages/twenty-sdk/src/sdk/define/objects/standard-object-ids.ts` (`export { STANDARD_OBJECTS as STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS } from 'twenty-shared/metadata';`). Replaced the "if not present" hedge in Task 4 Step 4 with the confirmed value and citation. Closed the corresponding Risks-section bullet with a strikethrough, matching the plan's existing convention for closed risks.

**N8 — cut table lists the workflow-manifest-unit cut twice. FIXED.**
Collapsed the two rows into one, folding in the second row's cheaper-cost framing ("the cost of the workaround dropped from... to one typed mutation call").

## Program document findings

**C14 — `AgentRun` workflow link and `Fact` freshness are in neither column. FIXED (program-doc scope only — Phase 2's plan file was not edited, per instructions).**
Added a new table under §6 naming both capabilities with a disposition: `AgentRun.workflowRunId` = BUILD (Phase 2 Task 4, with the mechanism named — an `AI_AGENT` workflow step already knows its run), `Fact.freshness` = CUT with a trigger (derive from `Evidence.observedAt` instead of a new unpopulated field), and flagged that Phase 2's success-criteria table currently makes a false "delivered" claim about it. Corrected §8's Execution and Evidence contract rows from "Satisfied after this review" to verdicts that name these two gaps explicitly and point back to §6, rather than silently omitting them.

**I25 — §2 C1 doesn't flag that Task 5 (one position earlier in the same merge order) independently destroys the same fields. FIXED.**
Appended a paragraph to the C1 row cross-referencing C9 (the plan-file review's finding) by name, stating plainly that Task 5's `buildGateInput` rewrite is a second, earlier break in the same file that the table's "resolved" language does not cover, and that the row must not be read as full resolution until Phase 4 Task 5 is separately repaired (out of this document's scope).

**I26 — §7 over-credits two narrative rows to `create_agent_task`. FIXED.**
Changed both the *Lead* step 3 row and the *Monitoring* step 1 row to lead with "**Partial**", naming both underlying reasons (no template prompt calls the tool; the tool doesn't compile as specified until Phase 2 Task 3 is repaired, C2 in the plan-file review).

**I27 — §5 omits `EvidenceSourceTypeGraphQL`. FIXED.**
Added a new component-ownership row for `EvidenceSourceTypeGraphQL`, owned by Phase 2 Task 11, consumed by Phase 2 Task 12's UI and any cross-phase GraphQL client, with the interface note that it must carry all seven `EvidenceSourceType` members (cross-referencing C4 in the plan-file review).

**I28 — §4's "zero merge risk" claim is true but implies zero build-order dependency, which is false pre-repair. FIXED.**
Added a correction to the bullet and to the Wave 2 (parallel C) table row: true as merge risk, but Task 8 originally could not complete (typecheck clean) in Wave 2 because its `post-install.ts` depended on Task 9, which depends on Phase 4 Task 10 (Wave 3/4). Noted this is now resolved by the same Phase 5 fix that closed I23 (moving the `post-install.ts` write into Task 9), so Task 8 is genuinely Wave-2-buildable post-repair, and Task 9 should be read as Wave 4 work, matching Phase 5's own "Depends on" section.

**Over-engineering cuts — checked against program doc, not previously reflected there. FIXED (added, not previously present to "update").**
Neither `AgentRunEntity.transcript`/`summarizeAgentSteps` nor `EvidenceLookupService`/`FactFieldsResolver`/`ProposalItemFieldsResolver` appeared anywhere in the program document before this pass (grepped both terms — zero hits outside one unrelated mention of "AgentMessage transcript machinery" in §6, which refers to a different reuse point and was left alone). Since §9 explicitly claims to be the union of all four plans' cut tables plus review-added items, added both cuts to §9's Phase 2 row with the same reasoning the plan-file review gives (replace `transcript` with `resultSummary`; replace the four-class stack with a single `ProposalItemDTO.facts` resolve field).

**§10 "what changed" table.** Extended the Phase 5 row with a full second-repair-pass summary listing every plan-file edit above, so §10 stays an accurate changelog.

## Disagreements with the review found during verification

- The review's C11 fix suggestion ("or move `installWorkflowDefinition` to a core-schema resolver") was not the direction taken — Phase 4's plan file already commits to `@MetadataResolver()` (verified, no contradiction found), so the correct-and-consistent fix is the other option the review itself offered: change the Phase 5 side to `MetadataApiClient`. No disagreement, just picking the branch that matches Phase 4's already-committed state.
- I24 could not be resolved to a definite yes/no the way N7 could — the review flagged this as inferred, and a second code search (this pass) still finds no explicit check of `canBeAssignedToAgents` anywhere reachable, but also no proof the flag is irrelevant. Recorded as still-open with the review's own named fallback applied, not fabricated as resolved.
- One minor addition beyond the assignment: I also updated the Wave 2 table row in §4 (not just the bullet list under "Parallelisation summary") for consistency, since both describe the same claim and only fixing one would leave the document self-contradictory.

## Not fully resolved / structurally blocked

- **I24** (see above) — genuinely open; Phase 4's agent-manifest install/validation code does not exist on disk yet, so no amount of reading the current repo resolves it. Fallback assertion added to Task 11 instead, with the field names it needs (`workflows { statuses }`, `agents { roleId }`) flagged as unverified against a schema that doesn't exist yet.
- **I21**'s exact `agents` query shape (filter-by-`universalIdentifier`) is written by analogy to other query patterns in the plan, not verified against a live resolver, for the same reason (Phase 4/agent GraphQL surface unbuilt). Flagged inline in the plan text.
- Per the assignment, Phase 4's Task 5 (C9) and Phase 2's Task 3/8/success-criteria table (C2, C14's Fact-freshness false claim) were **not edited** — they are out of scope for this repair pass and are cross-referenced instead, consistent with the instruction to not edit those plan files directly.

---

## Cross-plan contract correction

**Verified against:** `d:/Files/Vatsa/Projects/AI-CRM/twenty` at commit `0d127a81ff7ba623ce85d5222217a5df3021e0` (`git log -1 --format="%H %ci"` → `0d127a81ff7b 2026-08-06 09:00:54 +0530`), later than both repair passes above. `CoreApiClient` (`twenty-client-sdk/src/core/index.ts`) and `MetadataApiClient` (`twenty-client-sdk/src/metadata/index.ts`) confirmed as two distinct generated packages, matching prior verification. `AgentResolver` confirmed `@MetadataResolver()`-scoped at `packages/twenty-server/src/engine/metadata-modules/ai/ai-agent/agent.resolver.ts:29`. `Workflow` confirmed a workspace-entity standard object under `packages/twenty-server/src/modules/workflow/common/standard-objects/`, i.e. reachable only as a generic-object query on the core schema, not metadata.

**Root cause.** The earlier C11 repair pass (above, this file) and the separate Phase 4 repair pass ran in parallel against a mutation (`installWorkflowDefinition`) that neither plan had actually built yet — Phase 4 Task 10 is what creates it. Both passes had to *decide* the schema rather than read it off a resolver, and decided oppositely: this file's C11 entry put it on **metadata** (matching Phase 4's plan text *at the time this pass ran*); Phase 4's own later repair pass (`repair-phase4-report.md`, its C11 section) put it on **core**, via a new standalone `WorkflowDefinitionInstallResolver` (`@CoreResolver()`), specifically so Phase 5 keeps one `CoreApiClient` instead of adding a second generated client. Phase 4 owns the resolver, so Phase 4's decision governs, and this pass reverts C11 back to `CoreApiClient` per Phase 4 Task 10's "Contract exposed to Phase 5" table.

**What changed in `2026-08-05-phase-5-vertical-apps.md`:**
- `seedWorkflow`'s signature reverted to `client: CoreApiClient`, import from `twenty-client-sdk/core`.
- Task 9's Interfaces line and the Step 1 program-integration blockquote rewritten: `CoreApiClient` for `installWorkflowDefinition` (core schema, `WorkflowDefinitionInstallResolver`), `MetadataApiClient` kept but re-scoped to only the `agents` lookup query (which is genuinely metadata-scoped, `AgentResolver` verified `@MetadataResolver()`). The old note arguing for a metadata client is replaced, not deleted, with the correct rationale and the verified evidence (Phase 4's own contract table, `@CoreResolver()`/`@MetadataResolver()` tag mechanics).
- Both workflow-template functions (`seedNewTicketTriageWorkflow`, `seedSlaRiskSweepWorkflow`) now take **two** clients — `metadataClient` for the agent-id lookup, `coreClient` for `seedWorkflow` — since those two calls are genuinely on different schemas. This is a real signature change beyond a search-and-replace of the type name.
- `post-install.ts`: single `CoreApiClient` now serves `createSupportQueue` and both `seedWorkflow` calls; `metadataClient` is retained only for the agent lookups inside the two seeders, with an updated comment explaining why it's still needed.
- The C12 role note (line 262, outside Task 9) referenced the old `WorkflowTemplateResolver`/"metadata mutation" framing — corrected to `WorkflowDefinitionInstallResolver`/"core-schema mutation."
- Task 11's `'should install both workflow templates as ACTIVE'` integration test queried `workflows` via `MetadataApiClient` — wrong on two independent counts (wrong client for the mutation's schema, and `Workflow` is a workspace-entity standard object reachable only via `CoreApiClient` regardless). Switched to `coreClient`.

**C13 (step normalisation) — checked against the contract, no change needed.** Phase 4's final contract: steps need only `{ type, name, settings }`; `id` is generated server-side *where absent*, `valid` is *always* forced `true` server-side (supplied or not), and `nextStepIds` is chained in array order *unless explicitly authored*, in which case the authored value is preserved. Phase 5's templates supply `id` (a fixed UUID, matching the app's universal-identifier convention), `valid: true`, and `nextStepIds: null` (single-step workflows, so `null` is the correct terminal value either way). Checked each field against `normalizeWorkflowTemplateSteps`'s documented behavior (`repair-phase4-report.md` C13 section): an explicit `id` is preserved, not rejected; an explicit `valid: true` matches what the server forces regardless, so no conflict; an explicit `nextStepIds: null` on a single-step workflow is preserved as authored and is also what the server would generate for a last/only step. None of the three fields is ignored-with-surprise, rejected, or in conflict — supplying them is simply unnecessary, not wrong. Rather than strip them (which would have been the same category of unverified assumption this task exists to catch, just in the other direction), the plan text was corrected to state precisely why they're safe to keep: the type comment and the doc line above the code block now explain the optional/generated/preserved-if-authored contract instead of the old "id must be a fixed, hand-assigned UUID" phrasing, which overstated the requirement.

**C12 (WORKFLOWS permission flag) — confirmed intact and correctly named.** `app-default.role.ts` (Task 8) still declares `permissionFlagUniversalIdentifiers: [SystemPermissionFlag.WORKFLOWS]`, matching `SystemPermissionFlag.WORKFLOWS = '6189e7bd-4051-5752-b6b1-5f31358fbaf1'`, which is exactly what Phase 4's contract table requires of the caller. No change needed.

**Other Phase 4-owned assumptions checked against `repair-phase4-report.md` and the Phase 4 plan, and their outcome:**
- **Return shape** `{ workflowId, workflowVersionId }` — Phase 5's `seedWorkflow` return type and destructuring match. Held.
- **Idempotency by `name`** — Phase 5's post-install re-runs both seeders unconditionally on every upgrade (no `if (payload.previousVersion)` guard around the workflow seeds, unlike the queue seed). This matches the contract's explicit statement that "a post-install hook may re-run on every upgrade" because installation is keyed on name. Held, no change needed.
- **Input field name `activate`** — `seedWorkflow` sends `activate: true` inside the mutation args. Matches `InstallWorkflowDefinitionInput.activate: Boolean! = true`. Held.
- **Trigger shape sent as `{ name, type, settings }`** — Phase 4's `InstallWorkflowDefinitionInput.trigger` is typed as opaque `JSON`, so no server-side field-name contract to violate; Phase 5's `WorkflowTriggerTemplate` union is structurally plausible and explicitly caveated in-plan as unverified against the real `WorkflowTrigger` union. Not part of Phase 4's stated contract table, so left as previously written (already correctly hedged).
- **Module registration / where the mutation is reachable from** — not an assumption Phase 5's code encodes (it only calls a client method), so nothing to reconcile.
- **`agents` query for resolving `agentId`** — re-verified independently in this pass (not part of either repair's original C-numbered findings): `AgentResolver` is confirmed `@MetadataResolver()`-scoped, so `MetadataApiClient` is the correct client. This was already correct in the plan and required no change, but is called out here because it's exactly the kind of second silent mismatch this task was checking for, and it wasn't visible from the C11 diff alone (it lives on the *keep* side, the two workflow-template files' `agents.query` calls, not the *change* side).

**No other cross-plan mismatches found.** Scanned all `Phase 4` references in the Phase 5 plan (headline finding, Task 9 header/cut-rationale, "What was deliberately cut" table, open-questions section) — all describe Phase 4 ownership/sequencing correctly and needed no edits.
