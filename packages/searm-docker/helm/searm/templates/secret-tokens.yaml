{{- if .Values.secrets.tokens.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.tokens.name | default "tokens" }}
  namespace: {{ include "searm.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "searm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: tokens
stringData:
  accessToken: {{ include "searm.secret.tokens.access" . | quote }}
{{- end }}
