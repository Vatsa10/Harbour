---
title: Lokaler Server
description: Den lokalen SeaRM Docker-Server verwalten – starten, stoppen, aktualisieren, parallele Testinstanz und manuelle SDK-Einrichtung.
icon: server
---

## Lokalen Server verwalten

Verwenden Sie `yarn searm docker:*`, um den lokalen SeaRM-Container zu steuern:

| Befehl                                 | Was es tut                                          |
| -------------------------------------- | --------------------------------------------------- |
| `yarn searm docker:start`             | Server starten (lädt das Image bei Bedarf herunter) |
| `yarn searm docker:start 2.2.0`       | Eine bestimmte Serverversion starten                |
| `yarn searm docker:start --port 3030` | Auf einem benutzerdefinierten Port starten          |
| `yarn searm docker:stop`              | Server stoppen (Daten bleiben erhalten)             |
| `yarn searm docker:status`            | URL, Version und Anmeldedaten anzeigen              |
| `yarn searm docker:logs`              | Serverprotokolle streamen                           |
| `yarn searm docker:reset`             | Alle Daten löschen und neu starten                  |
| `yarn searm docker:upgrade`           | Das neueste `searm-app-dev`-Image herunterladen    |
| `yarn searm docker:upgrade 2.2.0`     | Auf eine bestimmte Version aktualisieren            |

Daten bleiben über Neustarts hinweg in zwei Docker-Volumes bestehen (`searm-app-dev-data` für PostgreSQL, `searm-app-dev-storage` für Dateien). Verwenden Sie `reset`, um alles zu löschen.

## Serverversion fixieren

Wenn keine Version übergeben wird, ermittelt `docker:start` die Version aus dem `engines.searm`-Bereich deiner App in der `package.json` – derselbe Bereich, gegen den der Server beim Installieren deiner App validiert. Es startet das neueste veröffentlichte `searm-app-dev`-Image, das den Bereich erfüllt, und greift auf `latest` zurück, wenn das Feld fehlt oder keine veröffentlichte Version übereinstimmt:

```json filename="package.json"
{
  "engines": {
    "searm": ">=2.2.0"
  }
}
```

Gib eine Version explizit an, um den Bereich für einen einzelnen Lauf zu überschreiben: `yarn searm docker:start 2.3.0`. Wenn bereits ein Container mit einer anderen Version existiert, aktualisiert `docker:start` ihn direkt (der Container wird neu erstellt, während deine Daten-Volumes erhalten bleiben).

## Aktualisieren des Server-Images

`yarn searm docker:upgrade` lädt das neueste Image herunter, vergleicht die Digests und erstellt den Container nur neu, wenn sich tatsächlich etwas geändert hat. Die Volumes bleiben erhalten — nur der Container wird ersetzt. Wenn ein neues Image heruntergeladen wurde und der Container lief, startet das Upgrade automatisch einen neuen Container; führen Sie anschließend `yarn searm docker:start` aus, um zu warten, bis er betriebsbereit ist.

```bash filename="Terminal"
yarn searm docker:upgrade            # Latest
yarn searm docker:upgrade 2.2.0      # Specific version
```

Überprüfen Sie die laufende Version mit `yarn searm docker:status` (dies zeigt die im Container enthaltene `APP_VERSION` an).

## Eine parallele Testinstanz ausführen

Übergeben Sie `--test` an jeden `docker:*`-Befehl, um eine zweite, vollständig isolierte Instanz zu verwalten — nützlich für Integrationstests oder Experimente, ohne Ihre Hauptentwicklungsdaten anzutasten:

| Befehl                              | Was es tut                                        |
| ----------------------------------- | ------------------------------------------------- |
| `yarn searm docker:start --test`   | Die Testinstanz starten (standardmäßig Port 2021) |
| `yarn searm docker:stop --test`    | Anhalten                                          |
| `yarn searm docker:status --test`  | Status anzeigen                                   |
| `yarn searm docker:logs --test`    | Protokolle streamen                               |
| `yarn searm docker:reset --test`   | Daten löschen                                     |
| `yarn searm docker:upgrade --test` | Image aktualisieren                               |

Die Testinstanz hat ihren eigenen Container (`searm-app-dev-test`), eigene Volumes (`searm-app-dev-test-data`, `searm-app-dev-test-storage`) und eine eigene Konfiguration — sie läuft parallel zu Ihrer Hauptinstanz ohne Konflikte. Kombinieren Sie `--test` mit `--port`, um den Port 2021 zu überschreiben.

## Manuelle Einrichtung (ohne Scaffolding-Tool)

Überspringen Sie das Scaffolding-Tool, wenn Sie das SDK zu einem bestehenden Projekt hinzufügen:

```bash filename="Terminal"
yarn add searm-sdk searm-client-sdk
```

Fügen Sie der `package.json` das Skript hinzu:

```json filename="package.json"
{
  "scripts": {
    "searm": "searm"
  }
}
```

Sie können jetzt `yarn searm dev`, `yarn searm docker:start` und den Rest ausführen.

<Note>
Installieren Sie `searm-sdk` nicht global — fixieren Sie es pro Projekt, damit jede App ihre eigene Version verwendet.
</Note>
