---
title: Server locale
description: Gestisci il server Docker SeaRM locale — avvia, arresta, aggiorna, istanza di test parallela e configurazione manuale dell'SDK.
icon: server
---

## Gestire il server locale

Usa `yarn searm docker:*` per controllare il container SeaRM locale:

| Comando                                | Cosa fa                                            |
| -------------------------------------- | -------------------------------------------------- |
| `yarn searm docker:start`             | Avvia il server (scarica l'immagine se necessario) |
| `yarn searm docker:start 2.2.0`       | Avvia una versione specifica del server            |
| `yarn searm docker:start --port 3030` | Avvia su una porta personalizzata                  |
| `yarn searm docker:stop`              | Arresta il server (conserva i dati)                |
| `yarn searm docker:status`            | Mostra URL, versione e credenziali di accesso      |
| `yarn searm docker:logs`              | Trasmetti in streaming i log del server            |
| `yarn searm docker:reset`             | Cancella i dati e riparti da zero                  |
| `yarn searm docker:upgrade`           | Scarica l'ultima immagine `searm-app-dev`         |
| `yarn searm docker:upgrade 2.2.0`     | Aggiorna a una versione specifica                  |

I dati persistono tra i riavvii in due volumi Docker (`searm-app-dev-data` per PostgreSQL, `searm-app-dev-storage` per i file). Usa `reset` per cancellare tutto.

## Blocco della versione del server

Quando non viene passata alcuna versione, `docker:start` risolve la versione dall'intervallo `engines.searm` della tua app in `package.json`, lo stesso intervallo rispetto al quale il server effettua la convalida quando la tua app viene installata. Avvia l'immagine `searm-app-dev` più recente pubblicata che soddisfa l'intervallo, ricorrendo a `latest` quando il campo è assente o nessuna versione pubblicata corrisponde:

```json filename="package.json"
{
  "engines": {
    "searm": ">=2.2.0"
  }
}
```

Passa esplicitamente una versione per ignorare l'intervallo per una singola esecuzione: `yarn searm docker:start 2.3.0`. Se un container esiste già con una versione diversa, `docker:start` lo aggiorna sul posto (ricreando il container ma preservando i volumi dei tuoi dati).

## Aggiornamento dell'immagine del server

`yarn searm docker:upgrade` scarica l'ultima immagine, confronta i digest e ricrea il container solo se è cambiato qualcosa. I volumi vengono preservati — viene sostituito solo il container. Se è stata scaricata una nuova immagine e il container era in esecuzione, l'upgrade avvia automaticamente un nuovo container; esegui poi `yarn searm docker:start` per attendere che diventi operativo.

```bash filename="Terminal"
yarn searm docker:upgrade            # Latest
yarn searm docker:upgrade 2.2.0      # Specific version
```

Verifica la versione in esecuzione con `yarn searm docker:status` (mostra `APP_VERSION` incorporata nel container).

## Esecuzione di un'istanza di test parallela

Passa `--test` a qualsiasi comando `docker:*` per gestire una seconda istanza completamente isolata — utile per test di integrazione o per sperimentare senza toccare i tuoi dati di sviluppo principali:

| Comando                             | Cosa fa                                                                  |
| ----------------------------------- | ------------------------------------------------------------------------ |
| `yarn searm docker:start --test`   | Avvia l'istanza di test (per impostazione predefinita usa la porta 2021) |
| `yarn searm docker:stop --test`    | Arrestala                                                                |
| `yarn searm docker:status --test`  | Mostra il suo stato                                                      |
| `yarn searm docker:logs --test`    | Trasmetti in streaming i suoi log                                        |
| `yarn searm docker:reset --test`   | Cancella i suoi dati                                                     |
| `yarn searm docker:upgrade --test` | Aggiorna la sua immagine                                                 |

L'istanza di test ha il proprio container (`searm-app-dev-test`), i propri volumi (`searm-app-dev-test-data`, `searm-app-dev-test-storage`) e la propria configurazione — viene eseguita insieme alla tua istanza principale senza conflitti. Combina `--test` con `--port` per sovrascrivere 2021.

## Configurazione manuale (senza lo scaffolder)

Salta lo strumento di scaffolding se stai aggiungendo l'SDK a un progetto esistente:

```bash filename="Terminal"
yarn add searm-sdk searm-client-sdk
```

Aggiungi lo script a `package.json`:

```json filename="package.json"
{
  "scripts": {
    "searm": "searm"
  }
}
```

Ora puoi eseguire `yarn searm dev`, `yarn searm docker:start` e il resto.

<Note>
Non installare `searm-sdk` globalmente — bloccalo per progetto in modo che ogni app usi la propria versione.
</Note>
