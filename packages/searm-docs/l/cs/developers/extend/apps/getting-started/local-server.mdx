---
title: Lokální server
description: Spravujte lokální Docker server SeaRM – spouštění, zastavení, upgrade, paralelní testovací instance a ruční nastavení SDK.
icon: server
---

## Správa lokálního serveru

K ovládání lokálního kontejneru SeaRM použijte `yarn searm docker:*`:

| Příkaz                                 | K čemu slouží                                  |
| -------------------------------------- | ---------------------------------------------- |
| `yarn searm docker:start`             | Spustí server (v případě potřeby stáhne image) |
| `yarn searm docker:start 2.2.0`       | Spustit konkrétní verzi serveru                |
| `yarn searm docker:start --port 3030` | Spustí na vlastním portu                       |
| `yarn searm docker:stop`              | Zastaví server (zachová data)                  |
| `yarn searm docker:status`            | Zobrazí URL, verzi a přihlašovací údaje        |
| `yarn searm docker:logs`              | Streamuje protokoly serveru                    |
| `yarn searm docker:reset`             | Vymaže data a začne znovu                      |
| `yarn searm docker:upgrade`           | Stáhne nejnovější image `searm-app-dev`       |
| `yarn searm docker:upgrade 2.2.0`     | Aktualizuje na konkrétní verzi                 |

Data přetrvávají při restartech ve dvou svazcích Dockeru (`searm-app-dev-data` pro PostgreSQL, `searm-app-dev-storage` pro soubory). Pomocí `reset` vymažte vše.

## Fixace verze serveru

Když není předána žádná verze, `docker:start` určí verzi z rozsahu `engines.searm` vaší aplikace v `package.json` — stejného rozsahu, vůči kterému server ověřuje, když je vaše aplikace nainstalována. Spustí nejnovější publikovaný image `searm-app-dev`, který splňuje tento rozsah, a pokud pole chybí nebo žádná publikovaná verze neodpovídá, použije `latest`:

```json filename="package.json"
{
  "engines": {
    "searm": ">=2.2.0"
  }
}
```

Předáním verze můžete rozsah pro jedno spuštění přepsat: `yarn searm docker:start 2.3.0`. Pokud už kontejner existuje v jiné verzi, `docker:start` jej na místě aktualizuje (znovu vytvoří kontejner při zachování vašich datových svazků).

## Aktualizace obrazu serveru

`yarn searm docker:upgrade` stáhne nejnovější image, porovná digesty a znovu vytvoří kontejner pouze v případě, že se skutečně něco změnilo. Svazky zůstanou zachovány — nahradí se pouze kontejner. Pokud byl stažen nový image a kontejner běžel, upgrade automaticky spustí nový kontejner; poté spusťte `yarn searm docker:start`, abyste počkali, než bude ve stavu 'healthy'.

```bash filename="Terminal"
yarn searm docker:upgrade            # Latest
yarn searm docker:upgrade 2.2.0      # Specific version
```

Běžící verzi ověříte pomocí `yarn searm docker:status` (zobrazí `APP_VERSION` zabudovanou v kontejneru).

## Spuštění paralelní testovací instance

Předejte `--test` libovolnému příkazu `docker:*` pro správu druhé, plně izolované instance — užitečné pro integrační testy nebo experimentování bez zásahu do vašich hlavních vývojových dat:

| Příkaz                              | K čemu slouží                                    |
| ----------------------------------- | ------------------------------------------------ |
| `yarn searm docker:start --test`   | Spustí testovací instanci (výchozí port je 2021) |
| `yarn searm docker:stop --test`    | Zastaví ji                                       |
| `yarn searm docker:status --test`  | Zobrazí její stav                                |
| `yarn searm docker:logs --test`    | Streamuje její protokoly                         |
| `yarn searm docker:reset --test`   | Vymaže její data                                 |
| `yarn searm docker:upgrade --test` | Aktualizuje její image                           |

Testovací instance má vlastní kontejner (`searm-app-dev-test`), svazky (`searm-app-dev-test-data`, `searm-app-dev-test-storage`) a konfiguraci — běží souběžně s vaší hlavní instancí bez konfliktů. Zkombinujte `--test` s `--port` pro změnu výchozího portu 2021.

## Ruční nastavení (bez generátoru kostry)

Pokud přidáváte SDK do existujícího projektu, generátor kostry přeskočte:

```bash filename="Terminal"
yarn add searm-sdk searm-client-sdk
```

Přidejte skript do `package.json`:

```json filename="package.json"
{
  "scripts": {
    "searm": "searm"
  }
}
```

Nyní můžete spouštět `yarn searm dev`, `yarn searm docker:start` a další.

<Note>
Neinstalujte `searm-sdk` globálně — nainstalujte jej v každém projektu zvlášť, aby každá aplikace používala svou vlastní verzi.
</Note>
