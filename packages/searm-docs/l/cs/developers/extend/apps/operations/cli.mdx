---
title: CLI
description: příkazy `yarn searm` pro spouštění funkcí, streamování logů, správu instalací aplikací a přepínání vzdálených serverů.
icon: terminal
---

CLI `yarn searm` je vaše rozhraní ke všemu, co souvisí s aplikací. Úplný seznam příkazů:

| Příkaz                                          | K čemu slouží                                                                       | Zdokumentováno v                                                                                         |
| ----------------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `dev`                                           | Sleduje zdrojové soubory a živě synchronizuje změny                                 | [Rychlý start](/l/cs/developers/extend/apps/getting-started/quick-start)                                      |
| `plán`                                          | Náhled změn metadat bez jejich aplikování                                           | [Synchronizace a obnovení](/l/cs/developers/extend/apps/operations/sync-and-recovery#previewing-changes-plan) |
| `použít`                                        | Aplikovat změny metadat po zobrazení plánu                                          | [Synchronizace a obnovení](/l/cs/developers/extend/apps/operations/sync-and-recovery)                         |
| `dev:build`                                     | Zkompilovat aplikaci a vygenerovat API klienta (`--tarball` pro zabalení do `.tgz`) | [Publikování](/l/cs/developers/extend/apps/operations/publishing)                                             |
| `dev:typecheck`                                 | Spustit kontrolu typů TypeScriptu                                                   | [Testování](/l/cs/developers/extend/apps/operations/testing)                                                  |
| `dev:add`                                       | Vytvořit kostru nové entity                                                         | [Scaffolding](/l/cs/developers/extend/apps/getting-started/scaffolding)                                       |
| `dev:generate-client`                           | Znovu vygenerovat typovaného API klienta                                            | tato stránka                                                                                             |
| `dev:function:exec` / `dev:function:logs`       | Spustit funkce a streamovat jejich logy                                             | tato stránka                                                                                             |
| `dev:translations-extract`                      | Extrahovat přeložitelné řetězce do katalogů v `locales/`                            | [Překlady](/l/cs/developers/extend/apps/translations/overview)                                                |
| `dev:catalog-sync`                              | Spustit synchronizaci katalogu tržiště                                              | [Publikování](/l/cs/developers/extend/apps/operations/publishing#how-marketplace-discovery-works)             |
| `app:publish` / `app:install` / `app:uninstall` | Životní cyklus vydání                                                               | [Publikování](/l/cs/developers/extend/apps/operations/publishing) a tato stránka                              |
| `docker:*`                                      | Spravovat kontejner lokálního serveru SeaRM                                        | [Lokální server](/l/cs/developers/extend/apps/getting-started/local-server)                                   |
| `remote:*`                                      | Spravovat připojení k serveru                                                       | tato stránka                                                                                             |

Každý příkaz přijímá `-r, --remote \<name>` pro zacílení na konkrétní vzdálené připojení místo výchozího.

## Spouštění funkcí (`yarn searm dev:function:exec`)

Spusťte logickou funkci ručně bez vyvolání přes HTTP, cron nebo databázovou událost:

```bash filename="Terminal"
# Execute by function name
yarn searm dev:function:exec -n create-new-post-card

# Execute by universalIdentifier
yarn searm dev:function:exec -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf

# Pass a JSON payload
yarn searm dev:function:exec -n create-new-post-card -p '{"name": "Hello"}'

# Execute the install and uninstall hooks
yarn searm dev:function:exec --postInstall
yarn searm dev:function:exec --preInstall
yarn searm dev:function:exec --uninstall
```

## Zobrazení logů funkcí (`yarn searm dev:function:logs`)

Streamujte výstupní logy běhu logických funkcí vaší aplikace:

```bash filename="Terminal"
# Stream all function logs
yarn searm dev:function:logs

# Filter by function name
yarn searm dev:function:logs -n create-new-post-card

# Filter by universalIdentifier
yarn searm dev:function:logs -u e56d363b-0bdc-4d8a-a393-6f0d1c75bdcf
```

<Note>
To se liší od `yarn searm docker:logs`, který zobrazuje logy kontejneru Docker. `yarn searm dev:function:logs` zobrazuje logy běhu funkcí vaší aplikace ze serveru SeaRM.
</Note>

## Generování typovaného klienta (`yarn searm dev:generate-client`)

Znovu vygenerujte typovaného klienta API (`searm-client-sdk`) ze schématu aktivního vzdáleného serveru, bez sestavování nebo synchronizace aplikace. Použijte jej k získání typovaného klienta v libovolném projektu – například backendové služby v samostatném repozitáři – který komunikuje s vaší instancí SeaRM:

```bash filename="Terminal"
# In your project (no SeaRM app definition required)
yarn add searm-sdk searm-client-sdk

# Connect to the SeaRM instance to generate the client from
yarn searm remote:add

# Generate the typed client into node_modules/searm-client-sdk
yarn searm dev:generate-client
```

Poté klienta importujte ve svém kódu:

```typescript
import { CoreApiClient } from 'searm-client-sdk/core';
```

Spusťte příkaz znovu pokaždé, když se změní váš datový model, abyste aktualizovali vygenerované typy.

<Note>
Klient je vygenerován uvnitř `node_modules`, takže není verzován spolu s vaším kódem. Spusťte `yarn searm dev:generate-client` po každé instalaci (například ve skriptu `postinstall` nebo v CI).
</Note>

## Odinstalace aplikace (`yarn searm app:uninstall`)

Odeberte svou aplikaci z aktivního pracovního prostoru:

```bash filename="Terminal"
yarn searm app:uninstall

# Skip the confirmation prompt
yarn searm app:uninstall --yes
```

## Správa vzdálených serverů

**Remote** je server SeaRM, ke kterému se vaše aplikace připojuje. Během nastavení jej generátor kostry automaticky vytvoří. Můžete kdykoli přidat další vzdálené servery nebo mezi nimi přepínat.

```bash filename="Terminal"
# Add a new remote (opens a browser for OAuth login)
yarn searm remote:add

# Connect to a local SeaRM server (auto-detects port 2020 or 3000)
yarn searm remote:add --local

# Add a remote non-interactively (useful for CI)
yarn searm remote:add --url https://your-searm-server.com --api-key $SEARM_API_KEY --as my-remote

# List all configured remotes
yarn searm remote:list

# Set the active remote
yarn searm remote:use <name>

# Check that the active remote's authentication is still valid
yarn searm remote:status

# Remove a remote
yarn searm remote:remove <name>
```

Vaše přihlašovací údaje jsou uloženy v `~/.searm/config.json`.
