{
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["searm-shared", "searm-shared/*"],
            "message": "searm-apps applications must not import from searm-shared directly. Import the runtime types and helpers you need from searm-sdk instead so the app bundle stays minimal."
          }
        ]
      }
    ]
  }
}
